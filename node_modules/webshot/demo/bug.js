var webshot = require('../lib/webshot');

webshot('github.com', 'github.png', {
    screenSize: {
        width: 1366,
        height: 768
    },
    shotSize: {
        width: 'window',
        height: 'all'
    }
}, function(err) {
  if (err) return console.log(err);
  console.log('OK');
});
